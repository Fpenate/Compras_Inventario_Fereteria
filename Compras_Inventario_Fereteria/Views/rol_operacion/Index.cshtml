@model IEnumerable<Compras_Inventario_Fereteria.Models.rol_operacion>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Rol Operaciones</h1>

<p>
    <button class="btn btn-success " onclick="location.href='@Url.Action("Create")';return false;">Crear Nuevo</button>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.operacioes.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.roles.nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.operacioes.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.roles.nombre)
        </td>
        <td>
            <button class="btn btn-info" onclick="location.href='@Url.Action("Edit",new { id = item.id_rol_operacion })';return false;">Editar</button>
            <button class="btn btn-default" onclick="location.href='@Url.Action("Details", new { id = item.id_rol_operacion })';return false;">Detalles</button>
            <button class="btn btn-danger" onclick="location.href='@Url.Action("Delete", new { id = item.id_rol_operacion })';return false;">Borrar</button>
        </td>
    </tr>
}

</table>

<button class="btn btn-default" onclick="location.href='@Url.Action("Index","Home")';return false;">Inicio</button>
